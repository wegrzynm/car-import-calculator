// This file can be used for global test setup
// For example, mocking global objects or libraries

// import { vi } from 'vitest';

// Example: Mocking a global function or API
// global.myGlobalFunction = vi.fn(() => 'mocked value');

// Ensure this file is referenced in vitest.config.ts under test.setupFiles
// console.log('Global test setup file loaded.');
